# Required
POSTGRES_PASSWORD=change-me
SECRET_KEY=change-me-generate-with-openssl-rand-hex-32

# Domain — used for CORS origins and SvelteKit ORIGIN
# Set to your domain when deploying behind Cloudflare Tunnel
DOMAIN=localhost

# Optional overrides
POSTGRES_USER=boat
POSTGRES_DB=boat
ACCESS_TOKEN_EXPIRE_MINUTES=480
# ALLOWED_ORIGINS=["https://boat.furkan8234.page"]
